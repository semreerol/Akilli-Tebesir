name: odev_takip
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Yardımcılar
  cupertino_icons: ^1.0.6
  intl: ^0.18.1
  
  # HIVE (Veritabanı)
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  
  # Kod Üreticiler
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true